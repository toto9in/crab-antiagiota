
valores bons que testei
WITH (m = 32, ef_construction = 400);

-- HNSW search efficiency
SET hnsw.ef_search = 60;

----

WITH (m = 32, ef_construction = 400);

-- HNSW search efficiency
SET hnsw.ef_search = 50;
